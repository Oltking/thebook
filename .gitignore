target/
**/*.wasm
# Stale build variants (the canonical thebook.idl stays tracked)
thebook-*.idl
.binpath
node_modules/

# Local agent skill packs (vara-skills router/bodies) — tooling, not project source
.agents/
.github/workflows/

# Build artifacts the deploy script copies next to itself
scripts/thebook.opt.wasm
scripts/thebook.idl

# Local agent tooling state
.claude/
.DS_Store
thebook-skills/

# Stray screenshots dropped in the repo root
Screenshot*.png
